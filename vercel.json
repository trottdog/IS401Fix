{
  "version": 2,
  "buildCommand": "npm run web:build",
  "outputDirectory": "static-build",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "^/api(?:/(.*))?$",
      "dest": "/api/[...all].ts"
    },
    {
      "src": "^/(.*)$",
      "dest": "/index.html"
    }
  ]
}
